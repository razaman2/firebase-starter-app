<script lang="jsx">
import ReactiveView from "@razaman2/reactive-view";

export default {
    setup() {
        return () => (
            <ReactiveView>Development Page</ReactiveView>
        );
    },
};
</script>
